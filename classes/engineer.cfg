// SPDX-License-Identifier: CC0-1.0

// <https://developer.valvesoftware.com/wiki/Bind>

bind   "mouse3"     "slot2"
bind   "mwheelup"   "slot1"
bind   "mwheeldown" "slot3"
bind   "mouse4"     "rebuild_sentry"
bind   "mouse5"     "rebuild_dispenser"

// Engineer and Spy are the only two classes where the mapped commands of "mouse3" and "mwheeldown" are switched.

// The commands "rebuild_sentry" and "rebuild_dispenser" are custom aliases. (see the file `custom_aliases.cfg`)
